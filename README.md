# gula (broken code)
An experimental `CLI` program build for nothing written in `C`

### Getting started

- Create `build` folder
```shell
$ mkdir build
```

- Run `cmake` command
```shell
$ cd build
$ cmake ..
```

- Build project
```shell
$ make
```

- Run
```shell
$ ./gula -h
```